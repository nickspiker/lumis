# Rust
/rust/target/
**/*.rs.bk

# Gradle / Android build
/.gradle/
/app/build/
/build/
local.properties

# APKs / signing artifacts
*.apk
*.aab
*.keystore
*.jks
*.hprof

# OS / editor
.DS_Store
*.swp
/app/libs/

# Local test assets - large 16-bit Rec.2020 reference TIFFs for P/Q demosaic tuning; not synced
/Artwork/

# Captured bias/dark calibration frames - large (~100MB each), device-specific; not synced
/dark frames/

# quad demosaic harness build output (source committed, build dir not)
/rust/quad_harness/target/
/rust/cal_harness/target/
